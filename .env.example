# ============================================
# Database Configuration
# ============================================
# Option 1: Use full DATABASE_URL (recommended)
DATABASE_URL="postgresql://user:password@localhost:5432/oly_portfolio?schema=public"

# Option 2: Use individual PostgreSQL variables (fallback if DATABASE_URL not set)
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="oly_portfolio"
POSTGRES_PORT="5432"
POSTGRES_HOST="localhost"

# ============================================
# JWT Authentication
# ============================================
# Generate a strong random string for production (min 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET="change-this-to-a-strong-random-string-in-production"

# ============================================
# Admin User Setup (for initial seed)
# ============================================
# These are used when running: npm run prisma:seed:admin
# Change these values before seeding in production!
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="admin123"

# ============================================
# Environment
# ============================================
# Set to 'production' for production deployment
NODE_ENV="development"
