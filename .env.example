# ============================================
# Database Configuration
# ============================================
# Option 1: Use full DATABASE_URL (recommended)
DATABASE_URL=

# Option 2: Use individual PostgreSQL variables (fallback if DATABASE_URL not set)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=
POSTGRES_HOST=
NEXTAUTH_SECRET=

# ============================================
# Admin User Setup (for initial seed)
# ============================================
# These are used when running: npm run prisma:seed:admin
# Change these values before seeding in production!
ADMIN_USERNAME=
ADMIN_PASSWORD=

# ============================================
# Environment
# ============================================
# Set to 'production' for production deployment
NODE_ENV="development"
APP_PORT=4123